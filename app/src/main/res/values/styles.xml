<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Overwrite cornerRadius for MaterialComponents TextInputLayout -->
    <style name="Rounded.TextInputLayout.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusBottomEnd">@dimen/dimen_8dp</item>
        <item name="boxCornerRadiusBottomStart">@dimen/dimen_8dp</item>
        <item name="boxCornerRadiusTopEnd">@dimen/dimen_8dp</item>
        <item name="boxCornerRadiusTopStart">@dimen/dimen_8dp</item>
    </style>

    <style name="DialogRoundedCorners" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/bg_rounded_dialog</item>
    </style>

</resources>
